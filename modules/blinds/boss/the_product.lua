SMODS.Blind{
    key = "the_product", --the count
    dollars = 5,
    mult = 2,
    boss = {min = 4, max = 32, showdown = false},
    boss_colour =HEX('528aae'),

    atlas = 'watsau_blinds', 
    pos = { x = 0, y = 2 },

    debuff = {
    
    }, 

    set_blind = function()

    end, 

    recalc_debuff = function(self, card, from_blind)

    end,


    drawn_to_hand = function(self)

    end,

    press_play = function(self)

    end
}
